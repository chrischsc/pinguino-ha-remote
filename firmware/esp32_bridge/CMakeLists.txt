# Ganymede bridge — ESP32-S3 Wi-Fi front-end (provisioning + web UI + UART to the nRF emulator).
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(ganymede_bridge)
